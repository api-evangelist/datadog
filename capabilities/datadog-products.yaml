naftiko: 1.0.0-alpha2
info:
  label: Datadog API — Products
  description: 'Datadog API — Products. 2 operations. Lead operation: Datadog Query Scalar Data Across Multiple Products.
    Self-contained Naftiko capability covering one Datadog business surface.'
  tags:
  - Datadog
  - Products
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    DATADOG_API_KEY: DATADOG_API_KEY
capability:
  consumes:
  - type: http
    namespace: datadog-products
    baseUri: https://{subdomain}.{site}
    description: Datadog API — Products business capability. Self-contained, no shared references.
    resources:
    - name: api-v2-query-scalar
      path: /api/v2/query/scalar
      operations:
      - name: queryscalardata
        method: POST
        description: Datadog Query Scalar Data Across Multiple Products
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: api-v2-query-timeseries
      path: /api/v2/query/timeseries
      operations:
      - name: querytimeseriesdata
        method: POST
        description: Datadog Query Timeseries Data Across Multiple Products
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.DATADOG_API_KEY}}'
  exposes:
  - type: rest
    namespace: datadog-products-rest
    port: 8080
    description: REST adapter for Datadog API — Products. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/api/v2/query/scalar
      name: api-v2-query-scalar
      description: REST surface for api-v2-query-scalar.
      operations:
      - method: POST
        name: queryscalardata
        description: Datadog Query Scalar Data Across Multiple Products
        call: datadog-products.queryscalardata
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/api/v2/query/timeseries
      name: api-v2-query-timeseries
      description: REST surface for api-v2-query-timeseries.
      operations:
      - method: POST
        name: querytimeseriesdata
        description: Datadog Query Timeseries Data Across Multiple Products
        call: datadog-products.querytimeseriesdata
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: datadog-products-mcp
    port: 9090
    transport: http
    description: MCP adapter for Datadog API — Products. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: datadog-query-scalar-data-across
      description: Datadog Query Scalar Data Across Multiple Products
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: datadog-products.queryscalardata
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: datadog-query-timeseries-data-across
      description: Datadog Query Timeseries Data Across Multiple Products
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: datadog-products.querytimeseriesdata
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
