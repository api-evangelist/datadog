naftiko: 1.0.0-alpha2
info:
  label: Datadog API — Resources
  description: 'Datadog API — Resources. 1 operations. Lead operation: Datadog List Confluent Account Resources. Self-contained
    Naftiko capability covering one Datadog business surface.'
  tags:
  - Datadog
  - Resources
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    DATADOG_API_KEY: DATADOG_API_KEY
capability:
  consumes:
  - type: http
    namespace: datadog-resources
    baseUri: https://{subdomain}.{site}
    description: Datadog API — Resources business capability. Self-contained, no shared references.
    resources:
    - name: api-v2-integrations-confluent-cloud-accounts-account_id-resources
      path: /api/v2/integrations/confluent-cloud/accounts/{account_id}/resources
      operations:
      - name: listconfluentresource
        method: GET
        description: Datadog List Confluent Account Resources
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.DATADOG_API_KEY}}'
  exposes:
  - type: rest
    namespace: datadog-resources-rest
    port: 8080
    description: REST adapter for Datadog API — Resources. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/api/v2/integrations/confluent-cloud/accounts/{account-id}/resources
      name: api-v2-integrations-confluent-cloud-accounts-account-id-resources
      description: REST surface for api-v2-integrations-confluent-cloud-accounts-account_id-resources.
      operations:
      - method: GET
        name: listconfluentresource
        description: Datadog List Confluent Account Resources
        call: datadog-resources.listconfluentresource
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: datadog-resources-mcp
    port: 9090
    transport: http
    description: MCP adapter for Datadog API — Resources. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: datadog-list-confluent-account-resources
      description: Datadog List Confluent Account Resources
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: datadog-resources.listconfluentresource
      outputParameters:
      - type: object
        mapping: $.
