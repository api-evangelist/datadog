naftiko: 1.0.0-alpha2
info:
  label: Datadog API — Acknowledge
  description: 'Datadog API — Acknowledge. 1 operations. Lead operation: Datadog Acknowledge On-call Page. Self-contained
    Naftiko capability covering one Datadog business surface.'
  tags:
  - Datadog
  - Acknowledge
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    DATADOG_API_KEY: DATADOG_API_KEY
capability:
  consumes:
  - type: http
    namespace: datadog-acknowledge
    baseUri: https://{subdomain}.{site}
    description: Datadog API — Acknowledge business capability. Self-contained, no shared references.
    resources:
    - name: api-v2-on-call-pages-page_id-acknowledge
      path: /api/v2/on-call/pages/{page_id}/acknowledge
      operations:
      - name: acknowledgeoncallpage
        method: POST
        description: Datadog Acknowledge On-call Page
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: page_id
          in: path
          type: string
          description: The page ID.
          required: true
    authentication:
      type: bearer
      token: '{{env.DATADOG_API_KEY}}'
  exposes:
  - type: rest
    namespace: datadog-acknowledge-rest
    port: 8080
    description: REST adapter for Datadog API — Acknowledge. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/api/v2/on-call/pages/{page-id}/acknowledge
      name: api-v2-on-call-pages-page-id-acknowledge
      description: REST surface for api-v2-on-call-pages-page_id-acknowledge.
      operations:
      - method: POST
        name: acknowledgeoncallpage
        description: Datadog Acknowledge On-call Page
        call: datadog-acknowledge.acknowledgeoncallpage
        with:
          page_id: rest.page_id
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: datadog-acknowledge-mcp
    port: 9090
    transport: http
    description: MCP adapter for Datadog API — Acknowledge. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: datadog-acknowledge-call-page
      description: Datadog Acknowledge On-call Page
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: datadog-acknowledge.acknowledgeoncallpage
      with:
        page_id: tools.page_id
      outputParameters:
      - type: object
        mapping: $.
