naftiko: 1.0.0-alpha2
info:
  label: Datadog API — Configure
  description: 'Datadog API — Configure. 1 operations. Lead operation: Datadog Configure Tags for Multiple Metrics. Self-contained
    Naftiko capability covering one Datadog business surface.'
  tags:
  - Datadog
  - Configure
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    DATADOG_API_KEY: DATADOG_API_KEY
capability:
  consumes:
  - type: http
    namespace: datadog-configure
    baseUri: https://{subdomain}.{site}
    description: Datadog API — Configure business capability. Self-contained, no shared references.
    resources:
    - name: api-v2-metrics-config-bulk-tags
      path: /api/v2/metrics/config/bulk-tags
      operations:
      - name: createbulktagsmetricsconfiguration
        method: POST
        description: Datadog Configure Tags for Multiple Metrics
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.DATADOG_API_KEY}}'
  exposes:
  - type: rest
    namespace: datadog-configure-rest
    port: 8080
    description: REST adapter for Datadog API — Configure. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/api/v2/metrics/config/bulk-tags
      name: api-v2-metrics-config-bulk-tags
      description: REST surface for api-v2-metrics-config-bulk-tags.
      operations:
      - method: POST
        name: createbulktagsmetricsconfiguration
        description: Datadog Configure Tags for Multiple Metrics
        call: datadog-configure.createbulktagsmetricsconfiguration
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: datadog-configure-mcp
    port: 9090
    transport: http
    description: MCP adapter for Datadog API — Configure. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: datadog-configure-tags-multiple-metrics
      description: Datadog Configure Tags for Multiple Metrics
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: datadog-configure.createbulktagsmetricsconfiguration
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
